export const metadata = {
  title: "Dashboard | HealthInHand",
  description: "Track your active fast, goal, streak, weekly fasting time, and weight trend."
};

export default function DashboardLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return children;
}
