export const metadata = {
  title: "Progress | HealthInHand",
  description: "View fasting trends, session summaries, and weight progress in HealthInHand."
};

export default function ProgressLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return children;
}
