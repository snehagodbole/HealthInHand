export const metadata = {
  title: "History | HealthInHand",
  description: "Review completed intermittent fasting sessions in HealthInHand."
};

export default function HistoryLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return children;
}
