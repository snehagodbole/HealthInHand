export const metadata = {
  title: "Login | HealthInHand",
  description: "Log in to your HealthInHand fasting tracker account."
};

export default function LoginLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return children;
}
