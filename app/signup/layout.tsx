export const metadata = {
  title: "Sign Up | HealthInHand",
  description: "Create a HealthInHand account to start tracking fasting routines."
};

export default function SignupLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return children;
}
